[build]
  publish = "."
  command = "node build.mjs"

[build.environment]
  NODE_VERSION = "20"

# Client-side password gate is in app.js.
# Optional stronger lock on Netlify Pro: Access control → Visitor access.
